# ByteDesk Site Templates

Canonical catalog for ByteDesk standardized HTML site templates.

Each catalog entry points to an individual `bytedesk-template-*` repository that contains a valid framework-neutral HTML package.
